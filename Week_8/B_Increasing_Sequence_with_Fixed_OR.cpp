#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // 1 2 4 8 16 32
    // 1 1 1 0 0  0    = 7
    // 0 1 0 0 1  0    = 18
    // 1 1 1 0 1 0     =(7|18) =  23

    // cout << (3 | 2);

    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;

        // cout << (1 << n) << endl;
        for (int i = 0; i < n; i++)
        {
            
        }
    }
    return 0;
}