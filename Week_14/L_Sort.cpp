#include <bits/stdc++.h>
using namespace std;

// Function to calculate the minimum number of operation you must perform
void solve()
{
    int n, q;
    cin >> n >> q;

    string a, b;
    cin >> a >> b;
   
    while (q--)
    {
        int l, r;
        cin >> l >> r;
        cout << l << " " << r << endl;
    }
}
int main()
{
    int t;
    cin >> t;
    while (t--)
        solve();
    return 0;
}